apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingress-alb-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ingress-alb-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingress-alb-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
