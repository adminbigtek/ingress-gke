apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingress-alb-chart.fullname" . }}
  labels:
    {{- include "ingress-alb-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingress-alb-chart.selectorLabels" . | nindent 4 }}
